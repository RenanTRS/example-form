import styled from "styled-components";

export const Input = styled.input`
    font-size: inherit;
    padding: 10px;
`;

export const InputM = styled.div`
    input{
        font-size: inherit;
        padding: 10px;
    }
`;